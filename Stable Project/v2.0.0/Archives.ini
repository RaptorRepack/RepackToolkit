        // Data-1 will be extracted. Change filename
        // We will use 90% since it has the 1st number of files.
        if not ShowChangeDiskWindow ('Data-1.dat is missing...', ExpandConstant('{src}'),'Data-1.dat') then break;
        if not ISArcExtract ( 0, 90, ExpandConstant('{src}\Data-1.dat'), ExpandConstant('{app}'), '', false, '', '', ExpandConstant('{app}'), notPCFonFLY {PCFonFLY}) then break;

        // Data-2 will be extracted. Change filename
        // We will use 10% since it has the 2end number of files.
        // 90 + 10 = 100%
        // Data-1.dat will be the 1st one to be extracted
        if not ShowChangeDiskWindow ('Data-2.dat is missing...', ExpandConstant('{src}'),'Data-2.dat') then break;
        if not ISArcExtract ( 0, 10, ExpandConstant('{src}\Data-2.dat'), ExpandConstant('{app}'), '', false, '', '', ExpandConstant('{app}'), notPCFonFLY {PCFonFLY}) then break;