        // Data will be extracted. Change filename
        // We will use 100% since it has the 1st number of files.
        if not ShowChangeDiskWindow ('Data.pkg is missing...', ExpandConstant('{src}'),'Data.pkg') then break;
        if not ISArcExtract ( 0, 100, ExpandConstant('{src}\Data.pkg'), ExpandConstant('{app}'), '', false, '', '', ExpandConstant('{app}'), notPCFonFLY {PCFonFLY}) then break;