        // Data will be extracted. Change filename
        // We will use 100% since it has the single number of files.
        // = 100%
        if not ShowChangeDiskWindow ('Data.tar is missing...', ExpandConstant('{src}'),'Data.tar') then break;
        if not ISArcExtract ( 0, 100, ExpandConstant('{src}\Data.tar'), ExpandConstant('{app}'), '', false, '', '', ExpandConstant('{app}'), notPCFonFLY {PCFonFLY}) then break;